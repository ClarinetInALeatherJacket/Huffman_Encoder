package huffman;

public class Decompressor {

    //int minCodeLength = compressionCodes.values().stream().mapToInt(String::length).min().orElse(1);
    //wint maxCodeLength = compressionCodes.values().stream().mapToInt(String::length).max().orElse(100);
    //
}
